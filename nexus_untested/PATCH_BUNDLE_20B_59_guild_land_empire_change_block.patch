--- server/metin2/Source/Server/game/src/char_change_empire.cpp
+++ server/metin2/Source/Server/game/src/char_change_empire.cpp
@@ -4,6 +4,7 @@
 #include "char_manager.h"
 #include "db.h"
 #include "guild_manager.h"
+#include "building.h"
 #include "marriage.h"
 #include "../../common/service.h"
 
@@ -22,6 +23,14 @@
 	if (GetEmpire() == empire)
 		return 1;
 
+	// HARDENING: do not allow empire change while owning guild land/buildings
+	if (GetGuild())
+	{
+		building::CLand* pkLand = building::CManager::instance().FindLandByGuild(GetGuild()->GetID());
+		if (pkLand)
+			return 2;
+	}
+
 	char szQuery[1024 + 1];
 	DWORD dwAID;
 	DWORD dwPID[PLAYER_PER_ACCOUNT];
@@ -91,11 +100,8 @@
 				MYSQL_ROW row = mysql_fetch_row(pMsg->Get()->pSQLResult);
 
 				str_to_number(dwGuildID[i], row[0]);
-
-				pGuild[i] = CGuildManager::instance().FindGuild(dwGuildID[i]);
-
-				if (pGuild[i] != NULL)
-					return 2;
+				// Any guild membership blocks empire change (do not rely on guild manager cache).
+				return 2;
 			}
 			else
 			{
