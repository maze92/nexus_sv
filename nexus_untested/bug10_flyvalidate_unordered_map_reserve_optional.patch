diff --git a/game/src/char.h b/game/src/char.h
--- a/game/src/char.h
+++ b/game/src/char.h
@@ -3318,7 +3318,10 @@ private:
 	DWORD m_dwMagicFlyValidatePendingCastSeq;
-	std::map<DWORD, TMagicFlyValidateCast> m_mapMagicFlyValidateCasts; // castSeq -> cast info
+	// Using unordered_map avoids tree overhead; reserve() prevents rehash spikes under bursty skill use.
+	// NOTE: Only apply this if you have already integrated Fly & Validate (m_mapMagicFlyValidateCasts).
+	std::unordered_map<DWORD, TMagicFlyValidateCast> m_mapMagicFlyValidateCasts; // castSeq -> cast info

diff --git a/game/src/char.cpp b/game/src/char.cpp
--- a/game/src/char.cpp
+++ b/game/src/char.cpp
@@ -186,6 +186,13 @@ CHARACTER::CHARACTER()
 {
 	m_stName = "";

+	// Fly & Validate pending cast map: pre-reserve to avoid rehash spikes during mass-combat.
+	// The typical concurrent pending count per character is small; 64 is a safe upper bound.
+	// NOTE: Only relevant if m_mapMagicFlyValidateCasts is an unordered_map.
+	m_mapMagicFlyValidateCasts.reserve(64);
+
 	CEntity::Initialize(ENTITY_CHARACTER);
