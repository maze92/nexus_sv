diff --git a/server/metin2/Source/Server/game/src/dungeon.cpp b/server/metin2/Source/Server/game/src/dungeon.cpp
index 9f7f386..e62e80a 100644
--- a/server/metin2/Source/Server/game/src/dungeon.cpp
+++ b/server/metin2/Source/Server/game/src/dungeon.cpp
@@ -51,6 +51,9 @@ void CDungeon::Initialize()
 	jump_to_event_ = NULL;
 	regen_id_ = 0;
 
+	m_bRegenInProgress = false;
+	m_dwLastRegenTick = 0;
+
 	m_iMobKill = 0;
 	m_iStoneKill = 0;
 	m_bUsePotion = false;
@@ -920,6 +923,21 @@ void CDungeon::SpawnRegen(const char* filename, bool bOnce)
 		return;
 	}
 
+	// Generic anti-spam / entity-limit guard (quest/UI agnostic).
+	// Prevents packet/NPC click spamming from spawning tens of thousands of entities.
+	const DWORD now = get_dword_time();
+	static const DWORD kRegenCooldownMs = 1200;
+	static const int kMaxDungeonMonstersBeforeBlock = 100;
+
+	if (m_bRegenInProgress)
+		return;
+
+	if (m_dwLastRegenTick && (now - m_dwLastRegenTick) < kRegenCooldownMs)
+		return;
+
+	if (CountMonster() >= kMaxDungeonMonstersBeforeBlock)
+		return;
+
 	LPSECTREE_MAP pkSectreeMap = SECTREE_MANAGER::instance().GetMap(m_lMapIndex);
 	if (!pkSectreeMap)
 	{
@@ -927,7 +945,10 @@ void CDungeon::SpawnRegen(const char* filename, bool bOnce)
 		return;
 	}
 
+	m_bRegenInProgress = true;
+	m_dwLastRegenTick = now;
 	regen_do(filename, m_lMapIndex, pkSectreeMap->m_setting.iBaseX, pkSectreeMap->m_setting.iBaseY, this, bOnce);
+	m_bRegenInProgress = false;
 }
 
 void CDungeon::AddRegen(LPREGEN regen)
@@ -1285,7 +1306,7 @@ struct FExitDungeonToStartPosition
 			{
 				PIXEL_POSITION posWarp;
 
-				// 현재 맵 인덱스를 넣는 것이 아니라 시작하는 맵 인덱스를 넣는다.
+				//   琯 獵  틈灸 求  琯 獵쨈.
 				if (SECTREE_MANAGER::instance().GetRecallPositionByEmpire(g_start_map[ch->GetEmpire()], ch->GetEmpire(), posWarp))
 					ch->WarpSet(posWarp.x, posWarp.y);
 				else
@@ -1444,7 +1465,7 @@ void CDungeon::JumpToEliminateLocation()
 	}
 	else
 	{
-		// 일반 맵으로 워프
+		// 球  
 		LPSECTREE_MAP pMap = SECTREE_MANAGER::instance().GetMap(m_lMapIndex);
 
 		if (!pMap)
diff --git a/server/metin2/Source/Server/game/src/dungeon.h b/server/metin2/Source/Server/game/src/dungeon.h
index 58f3b97..9f62672 100644
--- a/server/metin2/Source/Server/game/src/dungeon.h
+++ b/server/metin2/Source/Server/game/src/dungeon.h
@@ -71,8 +71,8 @@ public:
 
 	void IncMonster() { m_iMonsterCount++; /*sys_log(0, "MonsterCount %d", m_iMonsterCount);*/ }
 	void DecMonster() { m_iMonsterCount--; CheckEliminated(); }
-	int CountMonster() { return m_iMonsterCount; } // 데이터로 리젠한 몬스터의 수
-	int CountRealMonster(); // 실제로 맵상에 있는 몬스터
+	int CountMonster() { return m_iMonsterCount; } // 庫   
+	int CountRealMonster(); //  軻 獵 
 
 #if defined(__DUNGEON_RENEWAL__) || defined(__DEFENSE_WAVE__)
 	virtual void IncPartyMember(LPPARTY pParty, LPCHARACTER ch);
@@ -153,7 +153,7 @@ public:
 	void SetFlag(std::string name, int value);
 	void SetWarpLocation(long map_index, int x, int y);
 
-	// item group은 item_vnum과 item_count로 구성.
+	// item group item_vnum item_count .
 	typedef std::vector <std::pair <DWORD, int>> ItemGroup;
 	void CreateItemGroup(std::string& group_name, ItemGroup& item_group);
 	const ItemGroup* GetItemGroup(std::string& group_name);
@@ -197,7 +197,7 @@ private:
 	bool m_bExitAllAtEliminate;
 	bool m_bWarpAtEliminate;
 
-	// 적 전멸시 워프하는 위치
+	//   求 치
 	int m_iWarpDelay;
 	long m_lWarpMapIndex;
 	long m_lWarpX;
@@ -205,6 +205,8 @@ private:
 	std::string m_stRegenFile;
 
 	RegenVector m_regen;
+	bool m_bRegenInProgress;
+	DWORD m_dwLastRegenTick;
 
 	LPEVENT deadEvent;
 	// <Factor>
@@ -218,9 +220,9 @@ private:
 	friend EVENTFUNC(dungeon_exit_all_event);
 	friend EVENTFUNC(dungeon_jump_to_event);
 
-	// 파티 단위 던전 입장을 위한 임시 변수.
-	// m_map_pkParty는 관리가 부실하여 사용할 수 없다고 판단하여,
-	// 임시로 한 파티에 대한 관리를 하는 변수 생성.
+	// 티     擔 .
+	// m_map_pkParty  館臼   鳴 풔臼,
+	// 擔첨  티   求  .
 
 	LPPARTY m_pParty;
 
