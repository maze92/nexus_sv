--- /mnt/data/_patch_orig/entity_cpp_entity.cpp	2024-12-27 19:09:36.000000000 +0000
+++ "/mnt/data/nexus_src/Nexus OS/server/metin2/Source/Server/game/src/entity.cpp"	2026-02-17 05:22:05.652524195 +0000
@@ -2,6 +2,7 @@
 #include "char.h"
 #include "desc.h"
 #include "sectree_manager.h"
+#include "sectree.h"
 
 CEntity::CEntity()
 {
@@ -10,8 +11,13 @@
 
 CEntity::~CEntity()
 {
+	// In release builds (assert stripped), missing Destroy() leaks view/sectree references.
+	// Prefer to self-heal and log.
 	if (!m_bIsDestroyed)
-		assert(!"You must call CEntity::destroy() method in your derived class destructor");
+	{
+		sys_err("CEntity::~CEntity: Destroy() was not called (type=%d map=%ld)", m_iType, m_lMapIndex);
+		Destroy();
+	}
 }
 
 void CEntity::Initialize(int type)
@@ -36,6 +42,11 @@
 		return;
 
 	ViewCleanup();
+
+	// Ensure we are not left as an orphan inside the sectree.
+	if (m_pSectree)
+		m_pSectree->RemoveEntity(this);
+
 	m_bIsDestroyed = true;
 }
 
