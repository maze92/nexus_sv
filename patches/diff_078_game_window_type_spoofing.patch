--- a/server/metin2/Source/Server/game/src/char_item.cpp
+++ b/server/metin2/Source/Server/game/src/char_item.cpp
@@ -8267,6 +8267,20 @@
 			return false;
 		}
 
+
+#if defined(__DRAGON_SOUL_SYSTEM__)
+		// Prevent window-type spoofing: only Dragon Soul items may be placed into Dragon Soul equip slots.
+		if (DestCell.IsDragonSoulEquipPosition())
+		{
+			if (!item->IsDragonSoul())
+				return false;
+		}
+		else if (item->IsDragonSoul())
+		{
+			// Dragon Soul items cannot be equipped into normal equipment slots.
+			return false;
+		}
+#endif
 		EquipItem(item, DestCell.cell);
 	}
 	else

