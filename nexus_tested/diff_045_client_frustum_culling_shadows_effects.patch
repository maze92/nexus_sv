diff --git a/src-client/Client/GameLib/MapOutdoorRender.cpp b/src-client/Client/GameLib/MapOutdoorRender.cpp
--- a/src-client/Client/GameLib/MapOutdoorRender.cpp
+++ b/src-client/Client/GameLib/MapOutdoorRender.cpp
@@ -5,6 +5,7 @@ #include "TerrainQuadtree.h"
 
 #include "../EterLib/Camera.h"
+#include "../EterLib/GrpScreen.h"
 #include "../EterLib/StateManager.h"
 
 #define MAX_RENDER_SPALT 150
@@ -300,8 +301,42 @@ 
 struct FAreaRenderShadow
 {
-	void operator () (CGraphicObjectInstance* pInstance)
-	{
+	const Frustum& frustum;
+	D3DXVECTOR3 v3Eye;
+	float fMaxDistSq;
+
+	FAreaRenderShadow(const Frustum& f, const D3DXVECTOR3& eye, float fMaxDist)
+		: frustum(f), v3Eye(eye), fMaxDistSq(fMaxDist * fMaxDist)
+	{
+	}
+
+	void operator () (CGraphicObjectInstance* pInstance) const
+	{
+		if (!pInstance)
+			return;
+
+		D3DXVECTOR3 v3Center;
+		float fRadius = 0.0f;
+		if (pInstance->GetBoundingSphere(v3Center, fRadius))
+		{
+			const float dx = v3Center.x - v3Eye.x;
+			const float dy = v3Center.y - v3Eye.y;
+			const float dz = v3Center.z - v3Eye.z;
+			const float distSq = dx * dx + dy * dy + dz * dz;
+
+			if (distSq > fMaxDistSq)
+			{
+				pInstance->Hide();
+				return;
+			}
+
+			if (frustum.ViewVolumeTest(Vector3d(v3Center.x, v3Center.y, v3Center.z), fRadius) == VS_OUTSIDE)
+			{
+				pInstance->Hide();
+				return;
+			}
+		}
+
 		pInstance->RenderShadow();
 		pInstance->Hide();
 	}
@@ -428,7 +463,11 @@ 		STATEMANAGER.SaveTextureStageState(1, D3DTSS_ADDRESSV, D3DTADDRESS_BORDER);
 		STATEMANAGER.SaveTextureStageState(1, D3DTSS_BORDERCOLOR, 0xFFFFFFFF);
 
-		std::for_each(m_ShadowReceiverVector.begin(), m_ShadowReceiverVector.end(), FAreaRenderShadow());
+				CCamera* pCamera = CCameraManager::Instance().GetCurrentCamera();
+		const D3DXVECTOR3 v3Eye = pCamera ? pCamera->GetEye() : D3DXVECTOR3(0.0f, 0.0f, 0.0f);
+		const Frustum& frustum = CScreen::GetFrustum();
+		const float fMaxShadowDist = 20000.0f; // conservative
+		std::for_each(m_ShadowReceiverVector.begin(), m_ShadowReceiverVector.end(), FAreaRenderShadow(frustum, v3Eye, fMaxShadowDist));
 
 		STATEMANAGER.RestoreTextureStageState(1, D3DTSS_TEXCOORDINDEX);
 		STATEMANAGER.RestoreTextureStageState(1, D3DTSS_TEXTURETRANSFORMFLAGS);
@@ -547,7 +586,11 @@ 		//STATEMANAGER.SaveTextureStageState(1, D3DTSS_ADDRESSV, D3DTADDRESS_BORDER);
 		//STATEMANAGER.SaveTextureStageState(1, D3DTSS_BORDERCOLOR, 0xFFFFFFFF);
 
-		////std::for_each(m_ShadowReceiverVector.begin(), m_ShadowReceiverVector.end(), FAreaRenderShadow());
+		////		CCamera* pCamera = CCameraManager::Instance().GetCurrentCamera();
+		const D3DXVECTOR3 v3Eye = pCamera ? pCamera->GetEye() : D3DXVECTOR3(0.0f, 0.0f, 0.0f);
+		const Frustum& frustum = CScreen::GetFrustum();
+		const float fMaxShadowDist = 20000.0f; // conservative
+		std::for_each(m_ShadowReceiverVector.begin(), m_ShadowReceiverVector.end(), FAreaRenderShadow(frustum, v3Eye, fMaxShadowDist));
 
 		//STATEMANAGER.RestoreTextureStageState(1, D3DTSS_TEXCOORDINDEX);
 		//STATEMANAGER.RestoreTextureStageState(1, D3DTSS_TEXTURETRANSFORMFLAGS);
diff --git a/src-client/Client/GameLib/Area.cpp b/src-client/Client/GameLib/Area.cpp
--- a/src-client/Client/GameLib/Area.cpp
+++ b/src-client/Client/GameLib/Area.cpp
@@ -164,6 +164,13 @@ 	STATEMANAGER.SetRenderState(D3DRS_FOGENABLE, FALSE);
 #endif
 
+	// Tier 24: frustum + distance culling for effects (reduces CPU cost in crowded maps)
+	CCamera* pCamera = CCameraManager::Instance().GetCurrentCamera();
+	const D3DXVECTOR3 v3Eye = pCamera ? pCamera->GetEye() : D3DXVECTOR3(0.0f, 0.0f, 0.0f);
+	const Frustum& frustum = CScreen::GetFrustum();
+	const float fMaxEffectDist = 14000.0f; // conservative: keep far effects visible
+	const float fMaxEffectDistSq = fMaxEffectDist * fMaxEffectDist;
+
 	bool m_isDisableSortRendering = false;
 
 	if (m_isDisableSortRendering)
@@ -172,8 +179,26 @@ 		for (i = m_EffectInstanceMap.begin(); i != m_EffectInstanceMap.end();)
 		{
 			CEffectInstance* pEffectInstance = i->second;
+			++i;
+			if (!pEffectInstance)
+				continue;
+
+			D3DXVECTOR3 v3Center;
+			float fRadius = 0.0f;
+			if (pEffectInstance->GetBoundingSphere(v3Center, fRadius))
+			{
+				const float dx = v3Center.x - v3Eye.x;
+				const float dy = v3Center.y - v3Eye.y;
+				const float dz = v3Center.z - v3Eye.z;
+				const float distSq = dx * dx + dy * dy + dz * dz;
+				if (distSq > fMaxEffectDistSq)
+					continue;
+
+				if (frustum.ViewVolumeTest(Vector3d(v3Center.x, v3Center.y, v3Center.z), fRadius) == VS_OUTSIDE)
+					continue;
+			}
+
 			pEffectInstance->Render();
-			++i;
 		}
 	}
 	else
@@ -184,7 +209,28 @@ 		TEffectInstanceMap& rkMap_pkEftInstSrc = m_EffectInstanceMap;
 		TEffectInstanceMap::iterator i;
 		for (i = rkMap_pkEftInstSrc.begin(); i != rkMap_pkEftInstSrc.end(); ++i)
-			s_kVct_pkEftInstSort.push_back(i->second);
+		{
+			CEffectInstance* pEffectInstance = i->second;
+			if (!pEffectInstance)
+				continue;
+
+			D3DXVECTOR3 v3Center;
+			float fRadius = 0.0f;
+			if (pEffectInstance->GetBoundingSphere(v3Center, fRadius))
+			{
+				const float dx = v3Center.x - v3Eye.x;
+				const float dy = v3Center.y - v3Eye.y;
+				const float dz = v3Center.z - v3Eye.z;
+				const float distSq = dx * dx + dy * dy + dz * dz;
+				if (distSq > fMaxEffectDistSq)
+					continue;
+
+				if (frustum.ViewVolumeTest(Vector3d(v3Center.x, v3Center.y, v3Center.z), fRadius) == VS_OUTSIDE)
+					continue;
+			}
+
+			s_kVct_pkEftInstSort.push_back(pEffectInstance);
+		}
 
 		std::sort(s_kVct_pkEftInstSort.begin(), s_kVct_pkEftInstSort.end(), CArea_LessEffectInstancePtrRenderOrder());
 		std::for_each(s_kVct_pkEftInstSort.begin(), s_kVct_pkEftInstSort.end(), CArea_FEffectInstanceRender());
