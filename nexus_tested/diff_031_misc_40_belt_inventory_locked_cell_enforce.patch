--- a/game/src/char_item.cpp
+++ b/game/src/char_item.cpp
@@ -11109,7 +11109,20 @@
 #endif
 
 		case BELT_INVENTORY:
-			return cell < BELT_INVENTORY_MAX_NUM;
+		{
+			if (cell >= BELT_INVENTORY_MAX_NUM)
+				return false;
+
+			// Belt inventory has dynamically locked cells based on belt grade.
+			const LPITEM pBeltItem = GetWear(WEAR_BELT);
+			if (pBeltItem == nullptr)
+				return false;
+
+			if (cell < BELT_INVENTORY_SLOT_START)
+				return false;
+
+			return CBeltInventoryHelper::IsAvailableCell(cell - BELT_INVENTORY_SLOT_START, pBeltItem->GetValue(0));
+		}
 
 		case SAFEBOX:
 			if (NULL != m_pkSafebox)
